# include<stdio.h>
# include<math.h>
int main() 
{
	char ch  ; int count = 0 ;
    char name[100]= "saad is a good boy and a bad boy";
       for(int i =0 ; name[i] !='\0' ; i++)
    {
	    int no=0 ;
	   for(int j = 0 ; name[j] !='\0'; j++)
	   {
		   if(name[i] == name[j] && name[i] != ' ' )
			   no++;

	   
	   if(no > count)
	   {
		   count = no;
		   ch = name[i] ;
	   }
    }
    }

  printf("the highest occucred character is %c and it's count is %d", ch, count);
     
     return 0;

   
}
